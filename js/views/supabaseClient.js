// js/supabaseClient.js
// Template para integração com Supabase.
// Passos para integrar:
// 1) npm install @supabase/supabase-js (fora do GH Pages; no projeto local)
// 2) criar arquivo .env com SUPABASE_URL e SUPABASE_KEY (não comitar)
// 3) implementar e exportar o client abaixo substituindo a exportação de mockData
//
// Exemplo de uso (não ativo neste protótipo hospedado no GH Pages):
//
// import { createClient } from '@supabase/supabase-js'
// export const supabase = createClient(SUPABASE_URL, SUPABASE_KEY)
//
// Em seguida, crie adaptadores que mantenham as assinaturas:
// export async function getClients() { return supabase.from('clients').select('*') ... }

export const NOTE = 'Arquivo placeholder. Implementar supabase client localmente quando for integrar.';
